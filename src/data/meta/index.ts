import type { MetaRating } from '@/types';

/**
 * Meta ratings for heroes per game mode.
 *
 * EMPTY until verified community tier list data is ingested.
 * Do not invent tier lists, ratings, or usage statistics.
 * Meta ratings are kept SEPARATE from hero base properties.
 */
export const metaRatings: MetaRating[] = [];
